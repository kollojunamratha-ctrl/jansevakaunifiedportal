services:
  - type: web
    name: jansevak-backend
    env: node
    rootDir: .
    buildCommand: npm install
    startCommand: node backend/server.js
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 20
